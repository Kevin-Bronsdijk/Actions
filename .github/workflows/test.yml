name: test 1

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: message
        run: echo "Test 1,2,3"
      - name: multiple
        run: |
          node -v
          npm -v

  
